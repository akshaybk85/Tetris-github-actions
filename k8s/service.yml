apiVersion: v1 
kind: Service
metadata:
  name: Tetrics-Service
  namespace: default
spec:
  selector:
    app: Tetrics
  type: LoadBalancer
  ports:  
    - Portocol: TCP
      port: 80
      targetPort: 80